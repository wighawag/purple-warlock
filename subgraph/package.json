{
  "name": "purple-warlock-subgraph",
  "description": "Subgraph (thegraph.com) for Purple Warlock",
  "version": "0.1.0",
  "license": "UNLICENSED",
  "private": true,
  "devDependencies": {
    "@graphprotocol/graph-cli": "^0.16.0",
    "@graphprotocol/graph-ts": "^0.16.0",
    "cross-var": "^1.1.0",
    "dotenv-cli": "^3.1.0",
    "fs-extra": "^9.0.0",
    "handlebars": "^4.7.6",
    "wait-on": "^4.0.1"
  },
  "scripts": {
    "local:setup": "wait-on http-get://127.0.0.1:8000 && graph create purple-warlock/purple-warlock --node http://127.0.0.1:8020",
    "codegen": "rm -Rf generated && graph codegen",
    "build": "graph build",
    "generate": "node scripts/generate.js",
    "local:redeploy": "graph codegen && wait-on http://127.0.0.1:5001/api/v0/version http-get://127.0.0.1:8000 && graph deploy purple-warlock/purple-warlock --ipfs http://localhost:5001 --node http://127.0.0.1:8020",
    "local:deploy": "npm-run-all \"generate -- {1}\" local:redeploy --",
    "mainnet:deploy": "dotenv -- cross-var graph deploy --access-token $THEGRAPH_TOKEN purple-warlock/purple-warlock --ipfs https://api.thegraph.com/ipfs/ --node https://api.thegraph.com/deploy/ --debug"
  }
}
