{
  "build": {
    "image": "node:12",
    "command": "npm install --ignore-scripts && npm run prepare && cd web && npm run prepare && cd .. && npm run staging:web:build; echo done",
    "publicDir": "web/dist"
  }
 }
